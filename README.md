try { Elmah.ErrorLog.GetDefault(null).Log(new Elmah.Error(new Exception("DMS upload [" + DateTime.Now.ToString("yyyy-MM-dd HH:mm:ss.fff") + "] ENTERED UploadingDMSDocuments ClaimId=" + ClaimId + " Slno=" + Slno))); } catch { } // LOG
